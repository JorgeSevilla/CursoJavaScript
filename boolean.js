const isActive = true;
const hasPrmission = false;

//Conversión implícita
const result = 5 > 3;
console.log(result);
const name = 'Jorge';
console.log(!!name);

//Conversión explícita
const value = 0;
const otherValue = -24;
const explicitBoolean = Boolean(otherValue);
console.log(explicitBoolean);